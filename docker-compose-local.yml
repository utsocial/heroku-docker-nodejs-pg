client:
    build: .    
    command: node_modules/.bin/nodemon index.js
    dockerfile: DockerfileLocal
    environment:
        NODE_ENV: development
    ports:
        - "4000:4000"        
        - "5000:5000"        
    volumes:
        - .:/usr/src/app    
